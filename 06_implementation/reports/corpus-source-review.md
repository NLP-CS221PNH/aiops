# Coordinator review of source preparation

Reviewer: Codex coordinator (`/root`), automated technical review. Scope: `scripts/audit_corpus_sources.py`, its source registry/applicability output, and the source census report. This complements the separate agent review of the corpus implementation; it is not an A/B human applicability signature.

Outcome: pass for the authorized local candidate scope. The coordinator read the complete census implementation and its reports, checked the pinned source population/revisions/manifest hashes against the accepted plan and original snapshot, and reviewed the proposed exclusion reasons and conditional-use policy. The read-only source audit reports 5,666 checks, 74 documents (25/14/35), 12 assets and 3 license files. It checks decoded raw bytes, repository blob identities, acquisition receipts, source URL/revision/license metadata, source text/old chunk spans, and distinct commit/acquisition time semantics. A failing source audit prevents candidate building.

All 74 source records have reasons, explicit owners and blank human reviewer/time fields. Seven proposals exclude non-operational or unfinished source material; 67 remain unknown with document-specific preconditions. No row claims deployed compatibility or incident relevance. Title/shortcode/short-chunk findings are retained for content review. The source parser does not import or execute acquisition code or downloaded source examples. The builder hashes the remaining historical snapshot files as provenance without using proposed mapping text as index content.

The script writes only explicitly requested preparation/report outputs, defaults to read-only behavior, and refuses to overwrite existing human review fields. Its fixed source hashes and local allowlist prevent silently substituting the current snapshot. The tokenizer provisioning used separate public assets and an isolated package directory; the census itself performed no downloads.

No open source-preparation implementation finding was identified. Limits remain: the audit checks the recorded license evidence and its integrity, not legal permission approval; first-span evidence references and whole documents support proposals, not human judgments. Per-incident cutoff checks, actual deployment matching, semantic coverage and relevance remain downstream/unjudged. Real A/B applicability review and plan 02 acceptance are still required for release.
